{{> header }}

<div class="all-burgers"> 

    <div class="new">
        <h2>My Go-To Burgers</h2>

        {{#each burgers}} {{#unless devoured}}

            <div class="burger" data-id="{{id}}">
            <img src="./assets/img/cb.jpg" alt="">
            <p>{{burger_name}}</p>
            <button class="btn-default" data-id="{{id}}"> Delete </button>
            </div>

            {{/unless}} {{/each}}
    </div>
</div><br><br><br><br><br>